{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "node scripts/setup.mjs",
    "setup:netlify": "node scripts/setup.mjs --netlify",
    "postinstall": "node scripts/setup.mjs"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
